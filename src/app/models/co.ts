export interface Co {
    name: string;
    capital: string;
    region: string;
    population: number;
    area: number;
    flag: string;
    independent: boolean;
}

// https://restcountries.com/v2/name/peru?fields=name,capital,population,area,flag,region